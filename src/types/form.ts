export interface Form {
  marca: string;
  modelo: string;
  ano: string;
}

export interface Marca {
  codigo: string;
  nome: string;
}

export interface Modelo {
  codigo: string;
  nome: string;
}

export interface Ano {
  codigo: string;
  nome: string;
} 